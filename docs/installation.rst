============
Installation
============

At the command line::

    $ easy_install python-dpd-api

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-dpd-api
    $ pip install python-dpd-api
